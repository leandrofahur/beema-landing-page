import styled from 'styled-components';
import { Element } from 'react-scroll';

export const ReviewCardsContainer = styled.div`
  width: 350px;
  height: 450px;
  background-color: var(--white);
  box-shadow: 0px 0px 7px rgba(15, 15, 15, 0.2);
  border-radius: 3px;
  margin: 5px 0;
`;
